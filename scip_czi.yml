loading:
  format: czi
  channels: [0, 1]
  channel_labels:
    - DAPI
    - eGFP
  loader_kwargs:
    cell_diameter: 30
    dapi_channel: 0
    scene: "A2-A2"
masking:
  method:
feature_extraction:
  types: ["shape", "intensity", "bbox", "texture"]
export:
  format: parquet
  filename: features